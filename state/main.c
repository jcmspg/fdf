#include "header.h"

void *r(int x, char a)
{
    if (QE)

    return (NULL);
}

void *c(int x, char a)
{
    return (NULL);
}

void *w(int x, char a)
{
    return (NULL);
}

int main()
{
    t_fun s;
    t_fun r;
    t_s main_s;
    
    s.w = c;
    

    main_s.current = s;
    if (key == p)
        main_s.current = s;
    if (key == r)
        main_s.current = r;
    if (key == w)
        main_s.current.w(key, data);
    main_s.current.w(1, 'a');
    return 0;
}
